#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <ctype.h>
int _putchar(char c);
int is_positive_number(const char *str __attribute__((unused)));

#endif
